import React, { ReactElement } from 'react';

import style from 'components/playerStopper/PlayStopper.module.sass';

export const PlayStopper = (): ReactElement => <div className={style.container} />;
